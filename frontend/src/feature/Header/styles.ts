import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
    logout: {
            marginLeft: 'auto',
            textAlign: 'right',
      },
}));

export default useStyles;